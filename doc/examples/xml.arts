#
# Reads radiosondes ASCII file and saves it to XML
#

Main {
  ArrayOfMatrixReadAscii(abs_per_tg){"radiosondes.aa"}
  ArrayOfMatrixWriteXML(abs_per_tg){"radiosondes.xml"}

#  VectorReadAscii(f_mono) {"f_mono.aa"}
#  VectorWriteXML(f_mono) {"f_mono.xml"}
}
