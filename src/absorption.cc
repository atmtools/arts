// Stuff related to the calculation of absorption coefficients.

#include "arts.h"
#include "vecmat.h"

void AllAbsExample(// WS Output:
                   VECTOR& p_abs,
                   VECTOR& f_abs,
                   VECTOR& t_abs,
                   MATRIX& abs)
{
  cout << "AllAbsExample!\n";
}
