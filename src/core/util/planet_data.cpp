#include <planet_data.h>

namespace Body {
namespace Earth {
static_assert(a >= b);
}  // namespace Earth

namespace Jupiter {
static_assert(a >= b);
}  // namespace Jupiter

namespace Mars {
static_assert(a >= b);
}  // namespace Mars

namespace Moon {
static_assert(a >= b);
}  // namespace Moon

namespace Mercury {
static_assert(a >= b);
}  // namespace Mercury

namespace Venus {
static_assert(a >= b);
}  // namespace Venus

namespace Saturn {
static_assert(a >= b);
}  // namespace Saturn
}  // namespace Body
